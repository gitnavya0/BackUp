download service-account.json file from the google drive API page and add the path to that in the python scirpt  

also install - 

```pip install google-auth```

```pip install google-api-python-client```
